.DS_Store

# design handoff / reference material — not site source, kept local only
media/
*.zip
maybeit-work-design-spec.md

# npx writes an empty lockfile when it runs a local tool; not a dependency
package-lock.json
